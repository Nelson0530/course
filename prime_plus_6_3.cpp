#include <iostream>
using namespace std;
/*
const int MAX_LEN = 10000;             // 砞﹚

void rule_out_prime(int x, bool is_prime[], int n);
int main()
{
	int n;
	cin >> n;
	bool is_prime[MAX_LEN];               // 皚
	for (int i = 0; i < n; i++)
		is_prime[i] = true;                // 安砞硂皚┮Τ常琌借计(10000)

	for (int i = 2; i <= n; i++)
	{
		if (is_prime[i] == true)
		{
			cout << i << " ";
			rule_out_prime(i, is_prime, n);
		}
	}
	return 0;
}
void rule_out_prime(int x, bool is_prime[], int n)             //  x 竒琌借计┮ x * i 计﹚ぃ琌借计┮逼埃(false)
{
	for (int i = 1; x * i < n; i++)
		is_prime[x * i] = false;
}
*/